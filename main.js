const greeting = {
    message : "say Hi",
    times : 4
}
greeting.message = "Hai ges";
console.log(greeting.message)//=>"Hai ges"

const gambar = ['logo ig', 'logo tiktok'];

for (let i = 0; i < gambar.length; i++) {
    if (gambar) {
        console.log(gambar[i]);
    }
    else if (no) {
        console.log('Tidak ada data!!!');
    }
}

console.log(i);
